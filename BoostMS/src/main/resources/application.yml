# Configure Eureka Server
server:
  port: 8089

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8010/eureka

spring:
  application:
    name: BoostMS
  codec:
    max-in-memory-size: 1MB