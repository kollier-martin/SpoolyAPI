spring:
  application:
    name: BoostMS

  cloud:
    config:
      uri: http://localhost:1111
      fail-fast: true

  config:
    import:
      configserver: property

management:
  endpoints:
    web:
      exposure:
        include: *

# TODO: Implement Spring-Cloud-Bus and RabbitMQ for multiple service refreshing